# flask-ci-cd-lab
The CI/CD pipeline automatically builds the Flask application whenever code is pushed to the main branch. It installs dependencies, checks the application, and deploys it to Azure App Service. This automation ensures fast, consistent, and reliable application delivery.
